name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Tagged release build for Solarbank Load Manager"
